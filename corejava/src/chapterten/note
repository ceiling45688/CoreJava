1.JAR（java归档文件），使用ZIP压缩格式。
2.jar常用命令格式： jar options File1 File2 ..
options组合： cvf 创建新的存档文件并加入文件，生成详细的输出结果， 将jar文件名指定为第二个命令行参数
              x 解压文件  t 显示内容表
3.jar文件包括类文件、图像、其他资源，清单文件MANIFEST.MF（用于描述归档特征）
最基本标准：Manifest-Version：1.0 （其他条目需起始于明为Name的条目，节与节之间用空行分开）
4.密封：seal保证不会有其他类加入到包中。想要密封一个包，需要把包中的所有类放在一个jar文件中，默认jar中包不密封。
在MF文件主节中加入一行：Sealed: true  ，指定是否想要密封这个包。
5.沙箱sandbox：在沙箱中运行的代码不能修改或者查看用户系统。
限制：1).不能运行任何本地的可执行程序，
    2).不能从本地计算机文件系统中读取任何信息，也不能往本地计算机文件系统中写入任何信息；
    3).沙箱中的代码不能查看用户名，e-mail地址等信息；
    4).远程加载的程序不能与除下载程序所在的服务器之外的任何主机通信。
    5).所有的弹出式窗口都会带一个警告信息。
想要在沙箱外运行，java web start应用程序的jar文件必须进行数字签名。（见卷二）
6.关于java web start、JNLP API等内容以后再看。